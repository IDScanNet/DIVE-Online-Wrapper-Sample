import init from './UI/main/init';

// Step 1: App entry — application starts here and calls init() to bootstrap the UI flow.
// This will show the form and wait for user input (see src/UI/main/init.ts).
init();
